const planetLoader = require("./planetLoader");
const userLoader = require("./userLoader");
const challengeLoader = require("./challengeLoader");

module.exports = {
  planetLoader,
  userLoader,
  challengeLoader
};
